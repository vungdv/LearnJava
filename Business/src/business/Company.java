package business;
//package means protect the content inside.
public class Company {
    //Class should be in capitalize by java convention, it shouldn't be lowercase but don't do that
    //Method should be lowercase
    // Delete lines by command + delete
    FactoryWorker worker;
}
